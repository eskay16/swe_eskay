Task:
1. Pull.
2. create a new branch : task2
3. create an admin schema(model directory, should have its own '.js' file).
3.0. Create route (type get) once access that route it will generate an admin with the following credentials (email,password)
3.1. Ability to login as an admin(use credentials generated above).
3.2. Create a route that allows logged in admins to add other admins.
3.3. Admin should have a user interface where they can add other admins(email,password).
3.4. Admin should be able to delete other admins (UI & ROUTE).

4. All routes should be 'JWT' checked and validated except route from point 3.0.

